#include "function.h"

void esercizio_1(){
    
    double b = (d1 + 1) * std::pow(10, 20);
    double c = b * (-1);

    std::cout << "Valore di b = " << b << std::endl;
    std::cout << "valore di c = "<< c << std::endl;
}